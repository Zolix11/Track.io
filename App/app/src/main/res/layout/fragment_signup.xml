<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context=".ui.SignupFragment"
    android:background="@drawable/signup">

    <TextView
        android:id="@+id/textView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:text="SIGN UP FRAGMENT" />

    <Button
        android:id="@+id/btnSignupToLogin"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintBottom_toBottomOf="@id/btnSignupToHome"
        app:layout_constraintRight_toLeftOf="@id/btnSignupToHome"
        app:layout_constraintLeft_toLeftOf="parent"
        android:text="LOG IN"/>

    <Button
        android:id="@+id/btnSignupToHome"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintLeft_toRightOf="@id/btnSignupToLogin"
        android:text="HOME"/>

</androidx.constraintlayout.widget.ConstraintLayout>